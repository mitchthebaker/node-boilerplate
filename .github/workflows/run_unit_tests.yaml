name: Run Unit Tests 
on:
  pull_request:
    types: [opened, reopened]
    branches:
      - "main"
  pull_request_review:
    types: [submitted]
  push: 
    branches: 
      - "main"
      - "*smoke*"

jobs: 
  job-1:
    name: Unit test job 1 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 
      
      - name: Run playwright tests 
        run: |
          echo "in run playwright test job"